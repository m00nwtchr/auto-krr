apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: trailing-comment
  namespace: default
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      main:
        containers:
          app:
            image:
              repository: ghcr.io/example/app
              tag: 1.0
            env:
              - name: FOO
                value: bar
            resources:
              requests:
                cpu: 10m
            # krr: controller=main container=app
