apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: krr
  namespace: default
spec:
  chart:
    spec:
      chart: app-template
  values:
    controllers:
      krr:
        type: cronjob
