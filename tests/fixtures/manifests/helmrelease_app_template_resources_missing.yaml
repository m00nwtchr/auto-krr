apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: chronyd
  namespace: default
spec:
  chart:
    spec:
      chart: app-template
  values:
    controllers:
      main:
        containers:
          app:
            env: []
            image: ghcr.io/example/app:1.0
            securityContext: {}
